pid /tmp/nginx.pid;

events {
  worker_connections 128;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  access_log /dev/stdout;
  sendfile on;

  # Temp paths writable by the unprivileged `nginx` user.
  client_body_temp_path /tmp/client_temp;
  proxy_temp_path /tmp/proxy_temp;
  fastcgi_temp_path /tmp/fastcgi_temp;
  uwsgi_temp_path /tmp/uwsgi_temp;
  scgi_temp_path /tmp/scgi_temp;

  server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Fingerprintless but rare-changing static assets: cache hard, but not
    # forever, so a rebuilt image still reaches returning visitors.
    location /assets/ {
      add_header Cache-Control "public, max-age=604800";
    }

    # HTML may change with every rebuild: always revalidate.
    location ~* \.html$ {
      add_header Cache-Control "no-cache";
    }

    location / {
      try_files $uri $uri/ =404;
    }
  }
}
