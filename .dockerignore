# Keep the build context lean. The site image needs questions/, docs/, assets/,
# config/, the root pages, and scripts/build_site.py; the API image
# (Dockerfile.api) additionally needs api/, contentdb/, and requirements-api.txt.
.git
.github
.claude
build
dist
skills
tests
node_modules
__pycache__
*.pyc
.DS_Store
